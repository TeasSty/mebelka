---
import { site } from '../data/site';
import { withBase } from '../lib/paths';
import ResponsiveImage from './ResponsiveImage.astro';

const primaryPhone = site.phones.find((p) => p.primary) ?? site.phones[0];
---

<section class="hero" aria-labelledby="hero-title">
  <div class="container hero__shell">
    <div class="hero__content">
      <p class="hero__eyebrow">
        <span class="hero__eyebrow-mark" aria-hidden="true"></span>
        {site.city} · корпусная мебель на заказ
      </p>

      <h1 id="hero-title">{site.offer.headline}</h1>
      <p class="hero__lead">{site.offer.subline}</p>

      <ul class="hero__chips" aria-label="Преимущества">
        <li class="hero__chip">
          <span class="hero__chip-label">Рейтинг VK</span>
          <span class="hero__chip-value">{site.vk.rating}</span>
        </li>
        <li class="hero__chip">
          <span class="hero__chip-label">Замер</span>
          <span class="hero__chip-value">бесплатно</span>
        </li>
        <li class="hero__chip">
          <span class="hero__chip-label">Производство</span>
          <span class="hero__chip-value">своё</span>
        </li>
      </ul>

      <ul class="hero__list">
        <li>Кухни и шкафы-купе по вашим размерам</li>
        <li>Доставка и монтаж в {site.city}</li>
      </ul>

      <div class="hero__actions">
        <a class="btn btn--primary" href={withBase('/kontakty#zamer')}>{site.offer.cta}</a>
        <a class="btn btn--secondary" href={`tel:${primaryPhone.tel}`}>{primaryPhone.display}</a>
      </div>
    </div>

    <figure class="hero__visual">
      <div class="hero__mat">
        <ResponsiveImage
          name="hero-kitchen"
          alt="Кухня на заказ — работа МебельКа, Йошкар-Ола"
          width={1280}
          height={960}
          loading="eager"
          fetchpriority="high"
          sizes="(min-width: 960px) min(44vw, 520px), 100vw"
          class="hero__photo"
        />
      </div>
      <figcaption class="hero__caption">Фото из наших работ · <a href={site.vk.url}>ВКонтакте</a></figcaption>
    </figure>
  </div>
</section>

<style>
  .hero {
    padding-block: clamp(2rem, 5vw, 3.25rem);
    background:
      linear-gradient(180deg, var(--color-panel) 0%, var(--color-bg) 100%);
    border-bottom: 1px solid var(--color-line);
  }

  .hero__shell {
    display: grid;
    gap: var(--space-6);
    align-items: center;
  }

  .hero__content {
    max-width: 36rem;
  }

  .hero__eyebrow {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin: 0 0 var(--space-4);
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .hero__eyebrow-mark {
    flex-shrink: 0;
    width: 2rem;
    height: 3px;
    background: linear-gradient(90deg, var(--color-accent) 0%, var(--color-wood) 100%);
    border-radius: 1px;
  }

  .hero h1 {
    margin-bottom: var(--space-3);
    max-width: 18ch;
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    letter-spacing: -0.01em;
  }

  .hero__lead {
    margin-bottom: var(--space-5);
    max-width: 32rem;
    color: var(--color-muted);
    font-size: 1.0625rem;
    line-height: 1.55;
  }

  .hero__chips {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin: 0 0 var(--space-4);
    padding: 0;
    list-style: none;
  }

  .hero__chip {
    display: grid;
    gap: 0.125rem;
    padding: var(--space-2) var(--space-3);
    background: var(--color-panel);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-sm);
    min-width: 5.5rem;
  }

  .hero__chip-label {
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .hero__chip-value {
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--color-text);
    line-height: 1.2;
  }

  .hero__list {
    margin: 0 0 var(--space-5);
    padding: 0;
    list-style: none;
    display: grid;
    gap: var(--space-2);
    font-size: 0.9375rem;
    color: var(--color-text);
  }

  .hero__list li {
    padding-left: 1.125rem;
    position: relative;
  }

  .hero__list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.62em;
    width: 0.375rem;
    height: 1px;
    background: var(--color-wood);
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
  }

  .hero__visual {
    margin: 0;
    width: 100%;
    max-width: 32rem;
    justify-self: center;
  }

  .hero__mat {
    padding: var(--space-2);
    background: var(--color-panel);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-soft);
  }

  :global(.hero__photo) {
    width: 100%;
    aspect-ratio: 4 / 3;
    max-height: min(52vw, 22rem);
    object-fit: cover;
    border-radius: var(--radius-sm);
  }

  .hero__caption {
    margin-top: var(--space-2);
    padding-inline: var(--space-1);
    font-size: 0.75rem;
    color: var(--color-muted);
    text-align: center;
  }

  .hero__caption a {
    color: inherit;
    text-decoration: underline;
  }

  @media (min-width: 960px) {
    .hero__shell {
      grid-template-columns: minmax(0, 1fr) minmax(280px, 520px);
      gap: clamp(2rem, 4vw, 3.5rem);
    }

    .hero__visual {
      justify-self: end;
      max-width: none;
    }

    :global(.hero__photo) {
      max-height: 20rem;
    }
  }
</style>
